{
  "name": "@ImgProcessor-example/aws-php",
  "version": "0.0.0",
  "dependencies": {
    "@ImgProcessor/aws-s3": "workspace:*",
    "@ImgProcessor/core": "workspace:*",
    "@ImgProcessor/dashboard": "workspace:*",
    "ImgProcessor": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.17.1"
  },
  "private": true,
  "type": "module",
  "scripts": {
    "start": "php -S localhost:8080 serve.php",
    "outputBundle": "esbuild --format=esm --sourcemap=inline --bundle ./main.js"
  }
}
